{
  "name": "script",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "version:alpha": "ts-node src/version.ts prepatch",
    "version:patch": "ts-node src/version.ts patch",
    "version:minor": "ts-node src/version.ts minor",
    "version:major": "ts-node src/version.ts major",
    "changelog": "ts-node src/index.ts changelog",
    "github": "ts-node src/index.ts release",
    "release": "pnpm publish && pnpm github",
    "release:alpha": "pnpm version:alpha && pnpm changelog && pnpm release",
    "release:patch": "pnpm version:patch && pnpm changelog && pnpm release",
    "release:minor": "pnpm version:minor && pnpm changelog && pnpm release",
    "release:major": "pnpm version:major && pnpm changelog && pnpm release",
    "prepublishOnly": "pnpm build --filter vite-plugin-mkcert"
  },
  "devDependencies": {
    "execa": "^5.1.1",
    "fs-extra": "^10.0.0",
    "gh-release": "^6.0.0",
    "moment": "^2.29.1",
    "semver": "^7.3.5",
    "string-similarity": "^4.0.4",
    "ts-node": "^10.0.0"
  },
  "dependencies": {
    "@types/fs-extra": "^9.0.11",
    "@types/semver": "^7.3.6"
  }
}
